/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com",
            "ui-avatars.com",
            "localhost"
        ],
    },
    reactStrictMode: true,
};

export default nextConfig;
